spring.application.name=BuildingAutomationSystem


spring.datasource.url=jdbc:mysql://localhost:3306/buildingautomationsystem
spring.datasource.username=aritra
spring.datasource.password=Chiko2005#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# 2. JPA / HIBERNATE SETTINGS
# ===============================
# "update" means Spring will automatically create/modify tables to match your Java classes.
# Use "validate" or "none" for production.
spring.jpa.hibernate.ddl-auto=update

# Show the SQL queries in the console (Great for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.session.jdbc.initialize-schema=always

#mqtt broker settings
mqtt.broker.url=tcp://localhost:1883
mqtt.client.id=spring-boot-client
mqtt.default.topic=test-topic

# Change port to avoid conflict with the Zombie process
server.port=8080

youtube.api.key=AIzaSyDQsTRqfAzrTLY_1F44VKq--a5RURFeZpo


# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Optional but recommended: Set a Time-To-Live (TTL) so cache doesn't grow forever
spring.cache.redis.time-to-live=600000
